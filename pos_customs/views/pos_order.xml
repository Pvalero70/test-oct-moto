<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pos_order_cus_tree" model="ir.ui.view">
        <field name="name">view.pos.order.c.tree</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_total']" position="before">
                <field name="salesman_id" force_save="True" widget="many2one_avatar_user"/>
            </xpath>
        </field>
    </record>

 <!--<record id="view_pos_order_search" model="ir.ui.view">
        <field name="name">pos.order.search.view</field>
        <field name="model">pos.order</field>
        <field name="arch" type="xml">
            <search string="Point of Sale Orders">
                <field name="name"/>
                <field name="config_id"/>
            </search>
        </field>
    </record>-->
</odoo>