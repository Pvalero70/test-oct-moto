<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="sale_order_inherit_lot_line_form" model="ir.ui.view">
        <field name="name">sale.order.inherit.lot_line.form</field>
        <field name="model">sale.order</field>
        <field name="type">form</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='order_line']/tree/field[@name='price_unit']" position="before">
                <field name="original_name_line" invisible="1"/>
                <field name="original_price_unit" invisible="1"/>
                <field name="lot_domain_ids" widget="many2many_tags" invisible="1"/>
                <field name="lot_id" domain="[('id', 'in', lot_domain_ids)]"/>
            </xpath>
            <xpath expr="//field[@name='order_line']/tree/field[@name='price_unit']" position="attributes">
                <attribute name="force_save">1</attribute>
            </xpath>
        </field>
    </record>
</odoo>